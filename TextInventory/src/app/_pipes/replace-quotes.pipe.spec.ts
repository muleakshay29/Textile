import { ReplaceQuotesPipe } from './replace-quotes.pipe';

describe('ReplaceQuotesPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceQuotesPipe();
    expect(pipe).toBeTruthy();
  });
});
